# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/authdemo
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASS:Ravi@123}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# App
spring.application.name=authdemo
server.port=8089

# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6379

logging.file.name=logs/app.log
logging.level.com.example.authdemo=DEBUG
# ================= JWT CONFIG =================
# ================= JWT CONFIG =================
jwt.secret=MySuperSecretJWTKeyThatIsAtLeast32CharsLong
jwt.expiration=3600000
jwt.refreshExpiration=604800000
